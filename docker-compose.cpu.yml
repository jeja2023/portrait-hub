# 纯 CPU 部署 Docker Compose 配置（无 GPU / 无 CUDA 主机）。
# 与 docker-compose.yml 的区别：使用 Dockerfile.cpu 构建、移除所有 nvidia 设备预留，
# 并强制 FORCE_CPU=true。运行前建议复制 .env.example 为 .env 并配置鉴权/加密等参数。
# 启动：docker compose -f docker-compose.cpu.yml up -d --build

x-worker-environment: &worker-environment
  AUTH_REQUIRED: ${AUTH_REQUIRED:-true}
  DEBUG_ENDPOINTS_ENABLED: ${DEBUG_ENDPOINTS_ENABLED:-false}
  ENABLE_API_DOCS: ${ENABLE_API_DOCS:-false}
  TRUSTED_HOSTS: ${CPU_TRUSTED_HOSTS:-127.0.0.1,localhost,cpu-worker-0,portrait-stream-worker}
  TENANT_HEADER_REQUIRED: ${TENANT_HEADER_REQUIRED:-true}
  API_TOKEN: ${API_TOKEN:-}
  API_TOKEN_TENANT_ID: ${API_TOKEN_TENANT_ID:-}
  API_TOKEN_ALLOW_TENANT_OVERRIDE: ${API_TOKEN_ALLOW_TENANT_OVERRIDE:-false}
  RBAC_ENABLED: ${RBAC_ENABLED:-false}
  JWT_SECRET: ${JWT_SECRET:-}
  JWT_SECRET_ID: ${JWT_SECRET_ID:-primary}
  JWT_SECRET_KEYRING: ${JWT_SECRET_KEYRING:-}
  JWT_AUDIENCE: ${JWT_AUDIENCE:-portrait-hub-api}
  JWT_REQUIRE_EXP: ${JWT_REQUIRE_EXP:-true}
  JWT_REQUIRE_ISS: ${JWT_REQUIRE_ISS:-true}
  JWT_REQUIRE_AUD: ${JWT_REQUIRE_AUD:-true}
  JWT_REQUIRE_TENANT: ${JWT_REQUIRE_TENANT:-true}
  ENCRYPTION_KEY: ${ENCRYPTION_KEY:-}
  ENCRYPTION_KEY_ID: ${ENCRYPTION_KEY_ID:-primary}
  ENCRYPTION_KEYRING: ${ENCRYPTION_KEYRING:-}
  REQUIRE_ENCRYPTION: ${REQUIRE_ENCRYPTION:-true}
  MODEL_CONFIG_READ_FAIL_CLOSED: ${MODEL_CONFIG_READ_FAIL_CLOSED:-true}
  AUDIT_WRITE_FAIL_CLOSED: ${AUDIT_WRITE_FAIL_CLOSED:-true}
  STATE_READ_FAIL_CLOSED: ${STATE_READ_FAIL_CLOSED:-true}
  STATE_WRITE_FAIL_CLOSED: ${STATE_WRITE_FAIL_CLOSED:-true}
  PORTRAIT_RUNTIME_PROFILE: ${PORTRAIT_RUNTIME_PROFILE:-development}
  PRODUCTION_EXTERNAL_SERVICES_REQUIRED: ${PRODUCTION_EXTERNAL_SERVICES_REQUIRED:-true}
  PORTRAIT_STORAGE_BACKEND: ${PORTRAIT_STORAGE_BACKEND:-json}
  PORTRAIT_VECTOR_BACKEND: ${PORTRAIT_VECTOR_BACKEND:-local}
  PORTRAIT_REQUIRE_PRODUCTION_VECTOR_BACKEND: ${PORTRAIT_REQUIRE_PRODUCTION_VECTOR_BACKEND:-false}
  PORTRAIT_REQUIRE_PRODUCTION_MODEL_CAPABILITIES: ${PORTRAIT_REQUIRE_PRODUCTION_MODEL_CAPABILITIES:-false}
  PORTRAIT_OBJECT_STORAGE_BACKEND: ${PORTRAIT_OBJECT_STORAGE_BACKEND:-local}
  TASK_QUEUE_BACKEND: ${TASK_QUEUE_BACKEND:-local}
  TASK_QUEUE_DIR: ${TASK_QUEUE_DIR:-/workspace/runtime-state/task-queue}
  TASK_QUEUE_VISIBILITY_TIMEOUT_SECONDS: ${TASK_QUEUE_VISIBILITY_TIMEOUT_SECONDS:-300}
  TASK_QUEUE_POLL_INTERVAL_SECONDS: ${TASK_QUEUE_POLL_INTERVAL_SECONDS:-0.5}
  VIDEO_JOB_WORKER_IN_PROCESS: ${VIDEO_JOB_WORKER_IN_PROCESS:-false}
  RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-120}
  RATE_LIMIT_BURST: ${RATE_LIMIT_BURST:-240}
  RATE_LIMIT_MAX_BUCKETS: ${RATE_LIMIT_MAX_BUCKETS:-10000}
  RATE_LIMIT_BUCKET_TTL_SECONDS: ${RATE_LIMIT_BUCKET_TTL_SECONDS:-3600}
  MAX_REQUEST_BODY_BYTES: ${MAX_REQUEST_BODY_BYTES:-117440512}
  # 纯 CPU 部署：强制 CPU 推理，并保留自动回退作为双保险。
  FORCE_CPU: "true"
  CPU_FALLBACK_ENABLED: ${CPU_FALLBACK_ENABLED:-true}
  # 即使宿主机把 nvidia 设为默认运行时，也显式让容器看不到任何 GPU，避免误挂载。
  NVIDIA_VISIBLE_DEVICES: "none"
  SECURITY_HEADERS_ENABLED: ${SECURITY_HEADERS_ENABLED:-true}
  CONTENT_SECURITY_POLICY: ${CONTENT_SECURITY_POLICY:-default-src 'self'; object-src 'none'; base-uri 'none'; frame-ancestors 'none'; form-action 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'}
  HSTS_ENABLED: ${HSTS_ENABLED:-true}
  HSTS_MAX_AGE_SECONDS: ${HSTS_MAX_AGE_SECONDS:-31536000}
  HSTS_INCLUDE_SUBDOMAINS: ${HSTS_INCLUDE_SUBDOMAINS:-true}
  HSTS_PRELOAD: ${HSTS_PRELOAD:-false}
  MAX_PUBLIC_METADATA_BYTES: ${MAX_PUBLIC_METADATA_BYTES:-16384}
  MAX_PUBLIC_METADATA_DEPTH: ${MAX_PUBLIC_METADATA_DEPTH:-6}
  MAX_PUBLIC_METADATA_KEYS: ${MAX_PUBLIC_METADATA_KEYS:-128}
  MAX_PUBLIC_METADATA_STRING_LENGTH: ${MAX_PUBLIC_METADATA_STRING_LENGTH:-2048}
  MAX_AUDIT_PAYLOAD_BYTES: ${MAX_AUDIT_PAYLOAD_BYTES:-32768}
  MAX_AUDIT_DEPTH: ${MAX_AUDIT_DEPTH:-6}
  MAX_AUDIT_KEYS: ${MAX_AUDIT_KEYS:-128}
  MAX_AUDIT_LIST_ITEMS: ${MAX_AUDIT_LIST_ITEMS:-64}
  MAX_AUDIT_STRING_LENGTH: ${MAX_AUDIT_STRING_LENGTH:-2048}
  API_LIST_DEFAULT_LIMIT: ${API_LIST_DEFAULT_LIMIT:-100}
  MAX_API_LIST_LIMIT: ${MAX_API_LIST_LIMIT:-500}
  STREAM_EVENT_LIST_DEFAULT_LIMIT: ${STREAM_EVENT_LIST_DEFAULT_LIMIT:-100}
  MAX_STREAM_EVENT_LIST_LIMIT: ${MAX_STREAM_EVENT_LIST_LIMIT:-200}
  PORTRAIT_JOBS_STATE_PATH: ${PORTRAIT_JOBS_STATE_PATH:-/workspace/runtime-state/portrait-jobs.json}
  PORTRAIT_ANALYSIS_ARCHIVE_DB_PATH: ${PORTRAIT_ANALYSIS_ARCHIVE_DB_PATH:-/workspace/runtime-state/portrait-analysis-archive.sqlite3}
  ANALYSIS_ARCHIVE_ENABLED: ${ANALYSIS_ARCHIVE_ENABLED:-true}
  ANALYSIS_ARCHIVE_PREVIEW_MAX_SIDE: ${ANALYSIS_ARCHIVE_PREVIEW_MAX_SIDE:-480}
  VIDEO_JOB_INPUT_DIR: ${VIDEO_JOB_INPUT_DIR:-/workspace/runtime-state/video-job-inputs}
  VIDEO_UPLOAD_CHUNK_BYTES: ${VIDEO_UPLOAD_CHUNK_BYTES:-1048576}
  VIDEO_SAMPLE_INTERVAL_SECONDS: ${VIDEO_SAMPLE_INTERVAL_SECONDS:-1.0}
  VIDEO_INFERENCE_BATCH_SIZE: ${VIDEO_INFERENCE_BATCH_SIZE:-16}
  MAX_VIDEO_FRAME_UPLOADS: ${MAX_VIDEO_FRAME_UPLOADS:-64}
  STREAM_SAMPLE_INTERVAL_SECONDS: ${STREAM_SAMPLE_INTERVAL_SECONDS:-1.0}
  STREAM_INFERENCE_BATCH_SIZE: ${STREAM_INFERENCE_BATCH_SIZE:-8}
  ACCESS_STATS_FLUSH_INTERVAL_SECONDS: ${ACCESS_STATS_FLUSH_INTERVAL_SECONDS:-5}
  PORTRAIT_STREAMS_STATE_PATH: ${PORTRAIT_STREAMS_STATE_PATH:-/workspace/runtime-state/portrait-streams.json}
  POSTGRES_DSN: ${POSTGRES_DSN:-}
  POSTGRES_CONNECT_TIMEOUT_SECONDS: ${POSTGRES_CONNECT_TIMEOUT_SECONDS:-3}
  QDRANT_URL: ${QDRANT_URL:-}
  QDRANT_API_KEY: ${QDRANT_API_KEY:-}
  S3_ENDPOINT_URL: ${S3_ENDPOINT_URL:-}
  S3_REGION: ${S3_REGION:-us-east-1}
  S3_BUCKET: ${S3_BUCKET:-}
  REDIS_URL: ${REDIS_URL:-}
  OPENTELEMETRY_ENABLED: ${OPENTELEMETRY_ENABLED:-false}
  OTEL_SERVICE_NAME: ${OTEL_SERVICE_NAME:-portrait-hub}
  OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}

x-cpu-worker-common: &cpu-worker-common
  build:
    context: .
    dockerfile: Dockerfile.cpu
    args:
      INSTALL_PROD_OPTIONAL: ${INSTALL_PROD_OPTIONAL:-false}
  restart: unless-stopped
  env_file:
    - .env
  volumes:
    # 模型配置以可写方式挂载，灰度切换/回滚写回后可持久化。
    - type: bind
      source: ${MODEL_CONFIG_HOST_FILE:-./models.yml}
      target: /workspace/models.yml
      bind:
        create_host_path: false
    # 宿主机模型目录只读挂载。
    - type: bind
      source: ${MODELS_HOST_DIR:-./models}
      target: /models
      read_only: true
    # 运行期审计日志等状态文件放在宿主机目录。
    - type: bind
      source: ${RUNTIME_STATE_HOST_DIR:-./runtime-state}
      target: /workspace/runtime-state
  networks:
    - cpu-bridge
  healthcheck:
    # FORCE_CPU 下 /ready 以 CPUExecutionProvider 判定就绪。
    test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/ready', timeout=3).read()\""]
    interval: 30s
    timeout: 5s
    retries: 3
    start_period: 30s

services:
  cpu-worker-0:
    <<: *cpu-worker-common
    container_name: cpu-worker-0
    hostname: cpu-worker-0
    ports:
      # 只绑定宿主机本地回环地址，避免直接暴露到外网。
      - "127.0.0.1:9001:8000"
    environment:
      <<: *worker-environment

  portrait-video-job-worker:
    <<: *cpu-worker-common
    container_name: portrait-video-job-worker
    hostname: portrait-video-job-worker
    command: ["python", "-m", "app.portrait_video_job_worker"]
    ports: []
    healthcheck:
      disable: true
    environment:
      <<: *worker-environment
      VIDEO_JOB_WORKER_IN_PROCESS: "false"
  portrait-stream-worker:
    <<: *cpu-worker-common
    container_name: portrait-stream-worker
    hostname: portrait-stream-worker
    command: ["python", "-m", "app.portrait_stream_worker_daemon"]
    ports: []
    healthcheck:
      disable: true
    environment:
      <<: *worker-environment

networks:
  cpu-bridge:
    # 业务容器加入这个 external network 后，可通过 http://cpu-worker-0:8000 调用推理服务。
    name: cpu-bridge
    driver: bridge
